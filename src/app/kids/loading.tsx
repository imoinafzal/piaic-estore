import LoadingComponent from '@/components/ui/LoadingComponent'
import React from 'react'

const Loading = () => {
  return (
    <LoadingComponent  isCarousel={true} cardLimit={6} />
  )
}

export default Loading